@extends('layouts.app')

@section('title', 'Dashboard')

@push('styles')
<style>
    body {
        background-color: #f8f9fa;
    }
    
    .dashboard-header {
        background: linear-gradient(135deg, #043E03 0%, #0A5C08 100%);
        color: white;
        padding: 2rem 0;
        border-radius: 15px;
        margin-bottom: 2rem;
        box-shadow: 0 4px 15px rgba(4, 62, 3, 0.2);
    }
    
    .stat-card {
        background: white;
        border-radius: 15px;
        padding: 1.5rem;
        box-shadow: 0 3px 10px rgba(0,0,0,0.1);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        border-left: 4px solid #043E03;
        margin-bottom: 1.5rem;
    }
    
    .stat-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 8px 25px rgba(0,0,0,0.15);
    }
    
    .stat-card.primary {
        border-left-color: #043E03;
    }
    
    .stat-card.success {
        border-left-color: #28a745;
    }
    
    .stat-card.warning {
        border-left-color: #ffc107;
    }
    
    .stat-card.info {
        border-left-color: #17a2b8;
    }
    
    .stat-icon {
        width: 50px;
        height: 50px;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1rem;
        font-size: 1.5rem;
    }
    
    .stat-icon.primary {
        background-color: rgba(4, 62, 3, 0.1);
        color: #043E03;
    }
    
    .stat-icon.success {
        background-color: rgba(40, 167, 69, 0.1);
        color: #28a745;
    }
    
    .stat-icon.warning {
        background-color: rgba(255, 193, 7, 0.1);
        color: #ffc107;
    }
    
    .stat-icon.info {
        background-color: rgba(23, 162, 184, 0.1);
        color: #17a2b8;
    }
    
    .stat-number {
        font-size: 2rem;
        font-weight: bold;
        color: #2c3e50;
        margin-bottom: 0.5rem;
    }
    
    .stat-label {
        color: #6c757d;
        font-size: 0.9rem;
    }
    
    .section-card {
        background: white;
        border-radius: 15px;
        padding: 1.5rem;
        box-shadow: 0 3px 10px rgba(0,0,0,0.1);
        margin-bottom: 2rem;
    }
    
    .section-header {
        margin-bottom: 1.5rem;
        border-bottom: 1px solid #eee;
        padding-bottom: 1rem;
    }
    
    .section-title {
        font-size: 1.25rem;
        margin-bottom: 0;
    }
    
    .section-title i {
        color: #043E03;
        margin-right: 0.5rem;
    }
    
    .empty-state {
        text-align: center;
        padding: 3rem;
    }
    
    .empty-state-icon {
        font-size: 3rem;
        color: #adb5bd;
        margin-bottom: 1rem;
    }
    
    .booking-card {
        border-radius: 15px;
        overflow: hidden;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
        height: 100%;
        border: 1px solid #eee;
    }
    
    .booking-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 10px 25px rgba(0,0,0,0.1);
    }
    
    .booking-body {
        padding: 1rem;
    }
    
    .status-badge {
        padding: 0.25rem 0.5rem;
        border-radius: 50px;
        font-size: 0.75rem;
        font-weight: 600;
        display: inline-flex;
        align-items: center;
        gap: 0.25rem;
    }
    
    .status-badge.status-pending {
        background-color: rgba(255, 193, 7, 0.15);
        color: #856404;
    }
    
    .status-badge.status-confirmed {
        background-color: rgba(40, 167, 69, 0.15);
        color: #155724;
    }
    
    .status-badge.status-completed {
        background-color: rgba(0, 123, 255, 0.15);
        color: #004085;
    }
    
    .status-badge.status-cancelled {
        background-color: rgba(220, 53, 69, 0.15);
        color: #721c24;
    }
    
    .btn-modern {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        font-weight: 500;
    }
    
    .slide-up {
        animation: slideUp 0.5s ease-out;
    }
    
    @keyframes slideUp {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }
    
    .dashboard-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem 1rem;
    }
    
    .welcome-header {
        background: linear-gradient(135deg, #043E03 0%, #0A5C08 100%);
        color: white;
        padding: 2rem;
        border-radius: 15px;
        margin-bottom: 2rem;
        position: relative;
        overflow: hidden;
    }
    
    .welcome-title {
        font-size: 1.75rem;
        font-weight: bold;
        margin-bottom: 0.5rem;
    }
    
    .welcome-subtitle {
        opacity: 0.9;
        margin-bottom: 0;
    }
    
    .fade-in {
        animation: fadeIn 0.8s ease-out;
    }
    
    @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
    }
    
    .action-btn {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.5rem 1rem;
        border-radius: 8px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.2s ease;
    }
    
    .action-btn.primary {
        background-color: #043E03;
        color: white;
    }
    
    .action-btn.primary:hover {
        background-color: #032902;
        box-shadow: 0 5px 15px rgba(4, 62, 3, 0.3);
    }
    
    .stats-container {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1rem;
        margin-bottom: 2rem;
    }
    
    .slide-up {
        animation: slideUp 0.8s ease-out;
    }
    
    @keyframes slideUp {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
    }
</style>
@endpush

@section('content')
<div class="dashboard-container">
    <div class="container">
        <!-- Welcome Header -->
        <div class="welcome-header fade-in">
            <div class="welcome-text">
                <h1 class="welcome-title">Selamat Datang, {{ Auth::user()->name }}!</h1>
                <p class="welcome-subtitle">Kelola booking dan aktivitas lapangan Anda dengan mudah</p>
            </div>
        </div>
        
        <!-- Stats Overview -->
        <div class="stats-container">
            <div class="stat-card primary slide-up">
                <div class="stat-icon primary">
                    <i class="fas fa-calendar-check"></i>
                </div>
                <div class="stat-number">{{ $totalBookings }}</div>
                <div class="stat-label">Total Booking</div>
            </div>
            
            <div class="stat-card success slide-up">
                <div class="stat-icon success">
                    <i class="fas fa-check-circle"></i>
                </div>
                <div class="stat-number">{{ $completedBookings }}</div>
                <div class="stat-label">Booking Selesai</div>
            </div>
            
            <div class="stat-card warning slide-up">
                <div class="stat-icon warning">
                    <i class="fas fa-clock"></i>
                </div>
                <div class="stat-number">{{ $pendingBookings }}</div>
                <div class="stat-label">Booking Tertunda</div>
            </div>
            
            <div class="stat-card info slide-up">
                <div class="stat-icon info">
                    <i class="fas fa-star"></i>
                </div>
                <div class="stat-number">{{ $totalReviews }}</div>
                <div class="stat-label">Ulasan Diberikan</div>
            </div>
        </div>
        
        <!-- Booking Mendatang -->
        <div class="section-card slide-up">
            <div class="section-header">
                <div class="d-flex justify-content-between align-items-center">
                    <h4 class="section-title">
                        <i class="fas fa-calendar-alt"></i>
                        Booking Mendatang
                    </h4>
                    <a href="{{ route('bookings.index') }}" class="btn btn-outline-primary btn-modern">
                        <span>Lihat Semua</span>
                    </a>
                </div>
            </div>
            <div class="px-4 pb-4">
                @if($upcomingBookings->count())
                    <div class="row">
                        @foreach($upcomingBookings as $booking)
                        <div class="col-lg-4 col-md-6 mb-4">
                            <div class="booking-card">
                                <div class="booking-body">
                                    <div class="d-flex justify-content-between align-items-start mb-3">
                                        <h6 class="card-title mb-0 fw-bold">{{ $booking->field->name ?? 'Field not found' }}</h6>
                                        <span class="status-badge status-{{ strtolower($booking->status) }}">
                                            <i class="fas fa-circle" style="font-size: 0.5rem;"></i>
                                            {{ ucfirst($booking->status) }}
                                        </span>
                                    </div>
                                    <div class="mb-3">
                                        <div class="d-flex align-items-center mb-2">
                                            <i class="fas fa-calendar-day me-2 text-primary"></i>
                                            <small class="fw-semibold">{{ $booking->date ? $booking->date->format('d M Y') : 'Tanggal tidak tersedia' }}</small>
                                        </div>
                                        <div class="d-flex align-items-center">
                                            <i class="fas fa-clock me-2 text-primary"></i>
                                            <small class="fw-semibold">{{ $booking->start_time }} - {{ $booking->end_time }}</small>
                                        </div>
                                    </div>
                                    <div class="d-flex justify-content-between align-items-center">
                                        @if($booking->payment_status == 'unpaid')
                                            <a href="{{ route('payments.show', $booking->id) }}" class="btn btn-danger btn-sm btn-modern">
                                                <span><i class="fas fa-credit-card"></i> Bayar</span>
                                            </a>
                                        @else
                                            <div></div>
                                        @endif
                                        <a href="{{ route('bookings.show', $booking->id) }}" class="btn btn-primary btn-sm btn-modern">
                                            <span>Detail</span>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                        @endforeach
                    </div>
                @else
                    <div class="empty-state">
                        <div class="empty-state-icon">
                            <i class="fas fa-calendar-plus"></i>
                        </div>
                        <h5 class="mb-2">Tidak ada booking mendatang</h5>
                        <p class="mb-4">Mulai booking lapangan favorit Anda sekarang!</p>
                        <a href="{{ route('fields.index') }}" class="action-btn primary">
                            <i class="fas fa-plus"></i>
                            <span>Buat Booking Baru</span>
                        </a>
                    </div>
                @endif
            </div>
        </div>

        <!-- Booking Terakhir -->
        <div class="section-card slide-up">
            <div class="section-header">
                <div class="d-flex justify-content-between align-items-center">
                    <h4 class="section-title">
                        <i class="fas fa-history"></i>
                        Riwayat Booking
                    </h4>
                    <a href="{{ route('bookings.index') }}" class="btn btn-outline-primary btn-modern">
                        <span>Lihat Riwayat Lengkap</span>
                    </a>
                </div>
            </div>
            <div class="px-4 pb-4">
                @if($recentBookings->count())
                    <div class="row">
                        @foreach($recentBookings as $booking)
                        <div class="col-lg-4 col-md-6 mb-4">
                            <div class="booking-card">
                                <div class="booking-body">
                                    <div class="d-flex justify-content-between align-items-start mb-3">
                                        <h6 class="card-title mb-0 fw-bold">{{ $booking->field->name ?? 'Field not found' }}</h6>
                                        <span class="status-badge status-{{ strtolower($booking->status) }}">
                                            <i class="fas fa-circle" style="font-size: 0.5rem;"></i>
                                            {{ ucfirst($booking->status) }}
                                        </span>
                                    </div>
                                    <div class="mb-3">
                                        <div class="d-flex align-items-center mb-2">
                                            <i class="fas fa-calendar-day me-2 text-primary"></i>
                                            <small class="fw-semibold">{{ $booking->date ? $booking->date->format('d M Y') : 'Tanggal tidak tersedia' }}</small>
                                        </div>
                                        <div class="d-flex align-items-center">
                                            <i class="fas fa-clock me-2 text-primary"></i>
                                            <small class="fw-semibold">{{ $booking->start_time }} - {{ $booking->end_time }}</small>
                                        </div>
                                    </div>
                                    <div class="d-flex justify-content-between align-items-center">
                                        <div class="d-flex gap-2">
                                            @if(in_array($booking->status, ['completed', 'confirmed']) && !$booking->review)
                                                <a href="{{ route('reviews.create-from-booking', $booking->id) }}" 
                                                   class="btn btn-warning btn-sm btn-modern">
                                                    <span><i class="fas fa-star"></i> Ulasan</span>
                                                </a>
                                            @elseif($booking->review)
                                                <span class="badge bg-success">
                                                    <i class="fas fa-star"></i> Sudah diulas
                                                </span>
                                            @endif
                                        </div>
                                        <a href="{{ route('bookings.show', $booking->id) }}" class="btn btn-primary btn-sm btn-modern">
                                            <span>Detail</span>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                        @endforeach
                    </div>
                @else
                    <div class="empty-state">
                        <div class="empty-state-icon">
                            <i class="fas fa-history"></i>
                        </div>
                        <h5 class="mb-2">Belum ada riwayat booking</h5>
                        <p class="mb-0">Riwayat booking Anda akan muncul di sini</p>
                    </div>
                @endif
            </div>
        </div>
    </div>
</div>
@endsection

@push('scripts')
<script>
document.addEventListener('DOMContentLoaded', function() {
    // Add animation delays for staggered effect
    const elements = document.querySelectorAll('.slide-up');
    elements.forEach((el, index) => {
        el.style.animationDelay = `${index * 0.1}s`;
    });
    
    // Add hover effects for stat cards
    const statCards = document.querySelectorAll('.stat-card');
    statCards.forEach(card => {
        card.addEventListener('mouseenter', function() {
            this.style.transform = 'translateY(-8px) scale(1.02)';
        });
        
        card.addEventListener('mouseleave', function() {
            this.style.transform = 'translateY(0) scale(1)';
        });
    });
});
</script>
@endpush
